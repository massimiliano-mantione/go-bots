package vision

func Process(millis int, rightValue int, leftValue int) {
	// log.Println("vision.Process", millis, rightValue, leftValue)
}

func Estimate() (intensity int, angle int) {
	intensity = 0
	angle = 0
	return
}
