package config

// MaxSpeed is the max speed
const MaxSpeed = 1000000

// MaxIrValue is the max ir value
const MaxIrValue = 80

// TrackTurnSpeed is the turning speed in track
const TrackTurnSpeed = MaxSpeed / 2

// SeekTurnSpeed is the turning speed in seek
const SeekTurnSpeed = MaxSpeed / 2

// TrackSpeed is the speed in track
const TrackSpeed = MaxSpeed
